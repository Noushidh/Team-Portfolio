// Simplified post-processing placeholder — chromatic aberration-like visual effect
// Using a subtle CSS filter approach since @react-three/postprocessing adds bundle weight

export function EffectComposer({ isDark }: { isDark: boolean }) {
  // Post-processing effects would go here with @react-three/postprocessing
  // For now the chromatic aberration is handled via CSS on the canvas container
  return null;
}

export function ChromaticAberration() {
  return null;
}
